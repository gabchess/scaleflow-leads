# The one key the pipeline reads from env. Signal pass only; scraping needs no key.
APIFY_TOKEN=

# Crunchbase: no key. Save a logged-in session as .crunchbase.storageState.json (see src/scrapers/crunchbase.ts).
# Clay: no key here. Runs through Clay's MCP server, configured in your agent host.
